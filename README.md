# Voidle - Idle Clicker
## 🚀 Стек технологий
* **Unity 6.2+**
* **Zenject** — Dependency Injection для слабой связанности компонентов.
* **R3** — Реактивные свойства и потоки данных для UI и логики.
* **Addressables** — Асинхронная загрузка ассетов и управление памятью.
* **MVVM (Model-View-ViewModel)** — Четкое разделение бизнес-логики и представления.
* **JSON** — Система сериализации для гибкого сохранения прогресса.

## 🏗 Архитектура

### 1. Model (Data Layer)
* **Storage-классы** (`MoneyStorage`, `LevelStorage`, `UpgradesStorage`): Хранят чистое состояние игры. Не знают о существовании UI.
* **Persistence**: Реализована система сохранения через интерфейс `IDataPersistence`.

### 2. ViewModel (Logic Layer)
* Использует `R3.ReactiveProperty` для уведомления UI об изменениях.
* Выполняет роль посредника: форматирует данные.
* Управляет асинхронной загрузкой иконок через `Addressables`, гарантируя выгрузку ресурсов в `Dispose`.

### 3. View (Presentation Layer)
* Классы на базе `MonoBehaviour`.
* Только визуализация: подписка на ViewModel и обновление UI-элементов.
